# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Fake Distinction' do
  use_frameworks!

  pod 'CSV.swift', '~> 2.4.3'
  pod 'SVProgressHUD'
  target 'Fake DistinctionTests' do
    inherit! :search_paths
  end

  target 'Fake DistinctionUITests' do
  end

end
